// ════════════════════════════════════════
// DATA
// ════════════════════════════════════════
const Q = {
  kyA1(){return[
    {text:'"Саламатсызбы" означает?',answers:['Здравствуйте','До свидания','Спасибо','Пожалуйста'],correct:0},
    {text:'"Один" по-кыргызски?',answers:['Бир','Эки','Үч','Төрт'],correct:0},
    {text:'"Кызыл" — это?',answers:['Красный','Зелёный','Жёлтый','Чёрный'],correct:0},
    {text:'"Рахмат" означает?',answers:['Спасибо','Извините','Пожалуйста','Привет'],correct:0},
    {text:'"Жакшы" означает?',answers:['Хорошо','Плохо','Быстро','Медленно'],correct:0},
  ]},
  kyGreet(){return[
    {text:'"Кечирересиз" означает?',answers:['Извините','Спасибо','Привет','Пока'],correct:0},
    {text:'"Мама" по-кыргызски?',answers:['Эне','Ата','Ага','Эже'],correct:0},
    {text:'"Сен кимсиң?"',answers:['Кто ты?','Где ты?','Что это?','Как ты?'],correct:0},
    {text:'Число "беш"?',answers:['5','4','6','3'],correct:0},
    {text:'"Эне" — это?',answers:['Мама','Папа','Брат','Сестра'],correct:0},
  ]},
  python(){return[
    {text:'Тип данных у True?',answers:['bool','int','str','float'],correct:0},
    {text:'Как объявить список?',answers:['x = []','x = {}','x = ()','x = <>'],correct:0},
    {text:'Что делает len()?',answers:['Длина объекта','Тип','Удаляет','Сортирует'],correct:0},
    {text:'Комментарий в Python?',answers:['# текст','// текст','/* */','-- текст'],correct:0},
    {text:'print(2 ** 3) выведет?',answers:['8','6','9','5'],correct:0},
  ]},
  js(){return[
    {text:'Объявить переменную ES6?',answers:['let x = 5','var x = 5','dim x','int x'],correct:0},
    {text:'typeof null вернёт?',answers:['"object"','"null"','"undefined"','"boolean"'],correct:0},
    {text:'Метод в конец массива?',answers:['push()','pop()','shift()','unshift()'],correct:0},
    {text:'Что такое Promise?',answers:['Объект для async','Тип переменной','Колбэк','Ключевое слово'],correct:0},
    {text:'getElementById?',answers:['document.getElementById("x")','window.get("x")','query("x")','find("x")'],correct:0},
  ]},
  html(){return[
    {text:'Тег для ссылки?',answers:['<a>','<link>','<href>','<url>'],correct:0},
    {text:'Заголовок H1?',answers:['<h1>','<head>','<header>','<title>'],correct:0},
    {text:'CSS цвет текста?',answers:['color','text-color','font-color','foreground'],correct:0},
    {text:'display:flex — это?',answers:['Гибкий контейнер','Скрытый блок','Float','Сетка'],correct:0},
    {text:'Жирный текст?',answers:['<b>','<bold>','<thick>','<em>'],correct:0},
  ]},
  algo(){return[
    {text:'Сложность бинарного поиска?',answers:['O(log n)','O(n)','O(n²)','O(1)'],correct:0},
    {text:'LIFO — это?',answers:['Стек','Очередь','Дерево','Граф'],correct:0},
    {text:'Что такое рекурсия?',answers:['Функция вызывает себя','Цикл','Массив','Сортировка'],correct:0},
    {text:'Bubble sort worst case?',answers:['O(n²)','O(n)','O(log n)','O(n log n)'],correct:0},
    {text:'Хеш-таблица?',answers:['Поиск по ключу','Сортировка','Дерево','Список'],correct:0},
  ]},
  en(){return[
    {text:'"apple" — это?',answers:['Яблоко','Апельсин','Банан','Груша'],correct:0},
    {text:'Артикль: ___ book',answers:['a','an','the','—'],correct:0},
    {text:'"Thank you"?',answers:['Спасибо','Привет','Пока','Извини'],correct:0},
    {text:'"beautiful"?',answers:['Красивый','Большой','Быстрый','Умный'],correct:0},
    {text:'"I love coding"?',answers:['Я люблю кодить','Я учусь','Он кодит','Мы учимся'],correct:0},
  ]},
  de(){return[
    {text:'"Hund"?',answers:['Собака','Кошка','Птица','Рыба'],correct:0},
    {text:'Мужской артикль?',answers:['der','die','das','den'],correct:0},
    {text:'"Guten Morgen"?',answers:['Доброе утро','Добрый день','Пока','Спасибо'],correct:0},
    {text:'Число "drei"?',answers:['3','4','5','2'],correct:0},
    {text:'"Ich bin"?',answers:['Я есть','Ты есть','Он есть','Мы'],correct:0},
  ]},
};

const TESTS = [
  {id:1,track:'ky',  emoji:'🇰🇬',tag:'Кыргызский',title:'Кыргызский: A1 — Алфавит',   desc:'Буквы, числа, базовые слова',  time:15,q:5,level:'Лёгкий', bolts:8, questions:Q.kyA1()},
  {id:2,track:'ky',  emoji:'🇰🇬',tag:'Кыргызский',title:'Кыргызский: A1 — Приветствия',desc:'Саламдашуу, числа, цвета',     time:15,q:5,level:'Лёгкий', bolts:8, questions:Q.kyGreet()},
  {id:3,track:'code',emoji:'🐍', tag:'Python',    title:'Python: Основы',             desc:'Переменные, типы, циклы',      time:15,q:5,level:'Лёгкий', bolts:8, questions:Q.python()},
  {id:4,track:'code',emoji:'🌐', tag:'JavaScript',title:'JavaScript Basics',          desc:'Функции, массивы, DOM',         time:20,q:5,level:'Средний',bolts:12,questions:Q.js()},
  {id:5,track:'code',emoji:'🏗️',tag:'HTML/CSS',  title:'HTML & CSS',                 desc:'Разметка, flex, стили',         time:10,q:5,level:'Лёгкий', bolts:8, questions:Q.html()},
  {id:6,track:'code',emoji:'🧮', tag:'Алгоритмы', title:'Алгоритмы',                  desc:'Сортировки, поиск, структуры', time:25,q:5,level:'Сложный',bolts:20,questions:Q.algo()},
  {id:7,track:'lang',emoji:'🇬🇧',tag:'English',   title:'English: Базовая лексика',  desc:'Слова, фразы',                  time:15,q:5,level:'Лёгкий', bolts:8, questions:Q.en()},
  {id:8,track:'lang',emoji:'🇩🇪',tag:'Deutsch',   title:'Deutsch: A1',               desc:'Артикли, числа, глаголы',       time:18,q:5,level:'Лёгкий', bolts:10,questions:Q.de()},
];

const SHOP_ITEMS = {
  boosts:[
    {id:'h1',   icon:'💡',name:'Подсказка ×3',      desc:'Убирает 2 неверных варианта', price:15},
    {id:'t1',   icon:'⏳',name:'Заморозка времени',  desc:'Стоп-таймер на 5 сек',        price:25},
    {id:'s1',   icon:'⏭️',name:'Пропуск вопроса',   desc:'Без штрафа',                  price:20},
    {id:'x2',   icon:'⚡',name:'Двойной XP',         desc:'×2 XP за следующий тест',     price:40},
    {id:'lx3',  icon:'❤️',name:'+3 жизни',           desc:'Мгновенно восстанови',         price:30},
  ],
  avatars:[
    {id:'av1',icon:'🤖',name:'Робот',    desc:'Технарь',          price:30},
    {id:'av2',icon:'🧙',name:'Волшебник',desc:'Мастер кода',      price:30},
    {id:'av3',icon:'🥷',name:'Ниндзя',   desc:'Молчаливый умник', price:30},
    {id:'av4',icon:'🧑‍🚀',name:'Астронавт',desc:'Без границ',     price:50},
    {id:'av5',icon:'🐉',name:'Дракон',   desc:'Легендарный',      price:100},
  ],
  titles:[
    {id:'ti1',       icon:'💻',name:'«Кодер»',      desc:'Звание разработчика',      price:60},
    {id:'ti2',       icon:'🌍',name:'«Полиглот»',   desc:'Мастер языков',            price:60},
    {id:'ti3',       icon:'🧠',name:'«Гений»',      desc:'Лучший из лучших',         price:120},
    {id:'pro_rainbow',icon:'🌈',name:'Радужный ник',desc:'Переливающийся @юзернейм', price:200},
  ],
  usernames:[
    {id:'u_boss',  icon:'@',name:'@boss',  desc:'Редкое имя',          price:500},
    {id:'u_evrika',icon:'@',name:'@evrika',desc:'Название платформы!',  price:2000},
    {id:'u_pro',   icon:'@',name:'@pro',   desc:'Статусное',            price:800},
    {id:'u_gg',    icon:'@',name:'@gg',    desc:'Короткое и стильное',  price:300},
  ],
  all(){return[...this.boosts,...this.avatars,...this.titles,...this.usernames]},
  find(id){return this.all().find(i=>i.id===id)},
};

const THEMES = [
  {id:'default',label:'🌑 Тёмный',cls:'th-dark'},
  {id:'light',  label:'☀️ Светлый',cls:'th-light'},
  {id:'ocean',  label:'🌊 Океан',  cls:'th-ocean'},
  {id:'grape',  label:'🍇 Виноград',cls:'th-grape'},
  {id:'aurora', label:'🌌 Сияние', cls:'th-aurora'},
];

const FRIENDS_DATA = [
  {name:'Алия Бекова',   handle:'@aliya_k', av:'👩',   xp:340,bolts:120,streak:5,level:3,course:'Python'},
  {name:'Дамир Сейтов',  handle:'@damir99', av:'👦',   xp:210,bolts:75, streak:2,level:2,course:'Кыргызский A1'},
  {name:'Зарина Омарова',handle:'@zarina_o',av:'👩‍💻',xp:580,bolts:310,streak:8,level:5,course:'SQL'},
];

const STUDENTS = [
  {name:'Алия Бекова',   handle:'@aliya_k', edu:'Школьное',xp:340,bolts:120,lives:8, tests:7, streak:5,status:'active'},
  {name:'Дамир Сейтов',  handle:'@damir99', edu:'Высшее',  xp:210,bolts:75, lives:10,tests:4, streak:2,status:'active'},
  {name:'Зарина Омарова',handle:'@zarina_o',edu:'Школьное',xp:580,bolts:310,lives:6, tests:12,streak:8,status:'active'},
  {name:'Бекзат Нуров',  handle:'@bekat',   edu:'Другое',  xp:90, bolts:20, lives:10,tests:2, streak:0,status:'inactive'},
  {name:'Аина Жаксыбек', handle:'@aina_j',  edu:'Высшее',  xp:420,bolts:180,lives:9, tests:9, streak:4,status:'active'},
];

const CHATS = [
  {id:1,name:'Алия Бекова',   av:'👩',  last:'Как там Python? 🐍'},
  {id:2,name:'Дамир Сейтов',  av:'👦',  last:'Скинь решение задачи'},
  {id:3,name:'Зарина Омарова',av:'👩‍💻',last:'Прошла SQL на 100%! 🏆'},
];

const KY_LESSONS = [
  {n:1,title:'Алфавит',      desc:'Буквы и звуки кыргызского'},
  {n:2,title:'Числа 1–10',   desc:'Бир, эки, үч...'},
  {n:3,title:'Приветствия',  desc:'Саламдашуу сөздөрү'},
  {n:4,title:'Цвета',        desc:'Кызыл, жашыл, сары...'},
  {n:5,title:'Семья',        desc:'Ата, эне, ага, эже...'},
  {n:6,title:'Дни недели',   desc:'Дүйшөмбү, шейшемби...'},
];

const FEATURES = [
  {ic:'⚙️',title:'Болтики',        desc:'Зарабатывай за тесты, трать на аватары, темы и привилегии.'},
  {ic:'❤️',title:'Система жизней', desc:'10 жизней / 10 часов. Ошибка = -1. Pro = безлимит.'},
  {ic:'🎮',title:'Игровой формат', desc:'Kahoot-стиль: таймер, 4 кнопки, XP за скорость ответа.'},
  {ic:'🏅',title:'Сезонные медали',desc:'Осень, зима, весна, лето — уникальные медали за курсы.'},
  {ic:'👥',title:'Друзья и чат',   desc:'Профили, прогресс друзей, чат для обсуждения курсов.'},
  {ic:'🎨',title:'5 тем',          desc:'Тёмная, светлая, океан, виноград, северное сияние.'},
  {ic:'🪖',title:'Персонаж Болт',  desc:'Чел в жёлтом мотошлеме. Реагирует на твой прогресс.'},
  {ic:'@', title:'Юзернеймы',      desc:'Уникальный @username. Pro = переливающийся ник. Аукцион имён.'},
  {ic:'🤖',title:'ИИ-поддержка',   desc:'Мгновенные ответы на вопросы о платформе и курсах.'},
];

// ════════════════════════════════════════
// STATE
// ════════════════════════════════════════
// ════════════════════════════════════════
// ТЕОРИЯ ПО JAVA
// ════════════════════════════════════════

const THEORY_LESSONS = [
    {
        id: 1,
        title: 'Переменные и примитивные типы данных',
        description: 'Основы хранения данных в Java: что такое переменная, 8 примитивных типов, стек и куча, приведение типов.',
        content: `================================================================================
                    УРОК 1. ПЕРЕМЕННЫЕ И ПРИМИТИВНЫЕ ТИПЫ ДАННЫХ В JAVA
================================================================================

1. ЧТО ТАКОЕ ПЕРЕМЕННАЯ И КАК ОНА УСТРОЕНА

Переменная в Java — это именованная область памяти, в которой хранится значение
определённого типа. Компилятор и JVM строго следят за типом данных: нельзя
положить строку в переменную, предназначенную для числа.

Объявление переменной состоит из двух частей:
    тип_данных имя_переменной;

Инициализация — присвоение начального значения:
    int age;          // объявление
    age = 25;         // инициализация
    // или одной строкой:
    double price = 19.99;

ВАЖНО: Локальные переменные (объявленные внутри метода) не имеют значения по
умолчанию. Компилятор запретит использовать переменную, пока вы явно не
присвоите ей значение. Это спасает от случайного использования "мусора".

    int x;
    System.out.println(x); // Ошибка компиляции: variable x might not have been initialized


2. ПРИМИТИВНЫЕ ТИПЫ ДАННЫХ — ОСНОВА ОСНОВ

Java — строго типизированный язык. Все данные делятся на примитивные и
ссылочные. Примитивы хранят само значение (не ссылку на объект), что делает
работу с ними быстрой и предсказуемой.

В Java ровно 8 примитивных типов. Они делятся на четыре группы:

2.1. Целочисленные типы

    Тип      Размер   Диапазон значений
    ------------------------------------------------------------
    byte     8 бит    от -128 до 127
    short    16 бит   от -32 768 до 32 767
    int      32 бит   от -2 147 483 648 до 2 147 483 647 (~ ±2.1 млрд)
    long     64 бит   от -9 223 372 036 854 775 808 до 9 223 372 036 854 775 807

    Примеры:
    byte b = 100;
    short s = 30000;
    int i = 2000000;
    long bigNumber = 3_000_000_000L;   // суффикс L обязателен для чисел > 2.1 млрд

    Примечания:
    - int — стандартный выбор для целых чисел. JVM оптимизирована под 32 бита.
    - long используйте, когда значение может выйти за пределы int.
    - byte и short редко используются в явном виде, кроме массивов и потоков.
      В арифметических выражениях они автоматически повышаются до int.

2.2. Числа с плавающей точкой

    Тип      Размер   Точность            Пример
    ------------------------------------------------------------
    float    32 бит   ~6-7 знаков         float f = 5.75f;
    double   64 бит   ~15 знаков          double d = 19.99;

    - double — тип по умолчанию для дробных чисел.
    - float требует обязательного суффикса f или F.

    КРИТИЧЕСКИ ВАЖНЫЙ НЮАНС:
    Не все дробные числа можно представить точно в двоичной системе. Это приводит
    к ошибкам округления.

        double result = 0.1 + 0.2;
        System.out.println(result);          // 0.30000000000000004
        System.out.println(result == 0.3);   // false

    СЛЕДСТВИЕ: Никогда не используйте float и double для денег!
    Для финансовых расчётов применяйте класс BigDecimal.

2.3. Символьный тип char

    Тип      Размер   Диапазон               Пример
    ------------------------------------------------------------
    char     16 бит   от 0 до 65535          char c = 'A';
                (Unicode символы)

    char letter = 'A';
    char digit = '9';
    char symbol = '\\u00A9';   // © через Unicode escape
    char heart = '❤';         // эмодзи может занимать два char (surrogate pair)

    В арифметике char ведёт себя как беззнаковое целое:
        char a = 'A';
        char b = (char) (a + 1);   // 'B'
        System.out.println((int) 'A'); // 65 (код в Unicode/ASCII)

2.4. Логический тип boolean

    Тип         Возможные значения
    ------------------------------------------------------------
    boolean     true или false

    Пример:
    boolean flag = true;

    В отличие от C/C++, где true — это 1, а false — 0, в Java boolean — это
    отдельный тип, несовместимый с целыми числами. Нельзя написать if (1).

    Размер boolean в памяти не определён спецификацией и зависит от JVM.


3. ГДЕ ЖИВУТ ПРИМИТИВЫ? STACK И HEAP

Это понимание разделяет новичка и человека, который осознаёт управление памятью.

    - Stack (Стек): локальные переменные методов (включая примитивы) и ссылки
      на объекты.
    - Heap (Куча): сами объекты (экземпляры классов) и их поля.

    Пример с пояснением памяти:
        public void demo() {
            int counter = 10;          // [STACK] выделено 4 байта, записано 10
            String text = "Hello";     // [STACK] ссылка text (8 байт на 64-бит)
                                       // [HEAP]  объект String "Hello"
        }

    Когда метод demo() завершается, его стековый кадр уничтожается, и переменная
    counter исчезает. Объект String в куче остаётся, пока его не соберёт
    Garbage Collector (если на него больше нет ссылок).


4. ПРЕОБРАЗОВАНИЕ (CASTING) ПРИМИТИВНЫХ ТИПОВ

Java автоматически выполняет расширяющее преобразование (widening), если нет
риска потери данных:

    int i = 100;
    long l = i;      // автоматически: int -> long (64 бита > 32 бита)
    double d = i;    // автоматически: int -> double

Сужающее преобразование (narrowing) требует явного указания типа в скобках,
так как возможна потеря данных или точности:

    double pi = 3.14159;
    int intPi = (int) pi;  // intPi = 3, дробная часть отброшена

    long big = 1_000_000_000_000L;
    int small = (int) big; // small = -727379968 (переполнение, мусор)

Правила приведения в выражениях:
    1. Если один операнд double, второй приводится к double.
    2. Иначе если один float, второй к float.
    3. Иначе если один long, второй к long.
    4. Иначе оба операнда приводятся к int (даже byte и short!).

    byte a = 10;
    byte b = 20;
    byte c = a + b; // ОШИБКА: a + b даёт int, нужен явный каст: byte c = (byte) (a + b);


5. ЗНАЧЕНИЯ ПО УМОЛЧАНИЮ ДЛЯ ПОЛЕЙ КЛАССА

В отличие от локальных переменных, поля класса (статические и нестатические)
автоматически инициализируются нулевыми значениями. Это гарантирует JVM при
создании объекта.

    Тип поля                Значение по умолчанию
    ------------------------------------------------------------
    byte / short / int / long   0
    float / double              0.0
    char                        '\\u0000' (нулевой символ)
    boolean                     false
    Ссылочный тип               null

    Пример:
        class Defaults {
            int number;        // 0
            boolean flag;      // false
            String text;       // null
        }


6. ЧИСЛОВЫЕ ЛИТЕРАЛЫ С ПОДЧЁРКИВАНИЕМ (JAVA 7+)

Для улучшения читаемости больших чисел можно использовать символ '_':

    int million = 1_000_000;
    long creditCard = 1234_5678_9012_3456L;
    double hexBytes = 0xFF_EC_DE_5E;

Подчёркивания игнорируются компилятором, их можно ставить только между цифрами.


7. ШПАРГАЛКА: ТИПИЧНЫЕ ОШИБКИ НОВИЧКОВ

    1. Забытый 'f' у float:
       float f = 3.14;   // ошибка компиляции (несовместимость типов)
       Правильно: 3.14f

    2. Сравнение double через ==:
       Никогда так не делайте! Используйте Math.abs(a - b) < 0.0001 или BigDecimal.

    3. Деление целых чисел:
       int x = 5 / 2;   // x = 2 (дробная часть отбрасывается, а не округляется)

    4. Выход за границы типа (overflow):
       int max = Integer.MAX_VALUE;
       max = max + 1;   // получится Integer.MIN_VALUE (циклический перенос)


8. ИТОГ ПО ТЕМЕ

- Примитивов 8. Они хранят значения в стеке (локально) или внутри объекта в куче.
- Тип определяет размер памяти и допустимые операции.
- Java строга к типам: неявно только расширение, сужение — только явно и с риском.
- Для дробных вычислений с высокой точностью используйте BigDecimal.

================================================================================`
    },
    {
        id: 2,
        title: 'Операторы',
        description: 'Арифметические, логические, побитовые операторы, приоритет, тернарный оператор.',
        content: `================================================================================
                    УРОК 2. ОПЕРАТОРЫ В JAVA
================================================================================

1. ЧТО ТАКОЕ ОПЕРАТОР И ОПЕРАНД

Оператор — это специальный символ или ключевое слово, которое выполняет
определённую операцию над одним, двумя или тремя операндами и возвращает
результат.

    int sum = 10 + 20;
    // 10 и 20 — операнды
    // + — оператор
    // = — оператор присваивания
    // sum — переменная, принимающая результат

Операторы в Java классифицируются по:
    - Количеству операндов: унарные (1), бинарные (2), тернарный (3)
    - Типу выполняемой операции: арифметические, сравнения, логические,
      побитовые, присваивания, сдвига и прочие


2. ОПЕРАТОР ПРИСВАИВАНИЯ (=) И КОМБИНИРОВАННЫЕ ПРИСВАИВАНИЯ

2.1. Простое присваивание

    int x;
    x = 5;   // переменной x присвоено значение 5

Оператор = всегда выполняется справа налево: сначала вычисляется правая часть,
затем результат записывается в левую переменную.

2.2. Комбинированные операторы (Compound Assignment)

    Оператор    Эквивалент
    ------------------------------------------------------------
    x += y      x = x + y
    x -= y      x = x - y
    x *= y      x = x * y
    x /= y      x = x / y
    x %= y      x = x % y

    Пример:
        int a = 10;
        a += 5;   // a = 15
        a *= 2;   // a = 30

ВАЖНОЕ ОТЛИЧИЕ от обычного присваивания: комбинированные операторы автоматически
выполняют сужающее приведение типов (narrowing cast), которое без них вызвало бы
ошибку компиляции.

    byte b = 10;
    b = b + 5;      // ОШИБКА: b + 5 даёт int
    b += 5;         // РАБОТАЕТ: эквивалентно b = (byte) (b + 5)


3. АРИФМЕТИЧЕСКИЕ ОПЕРАТОРЫ

3.1. Базовые: +, -, *, /, %

    int sum = 10 + 20;      // 30
    int diff = 50 - 15;     // 35
    int product = 6 * 7;    // 42
    int quotient = 10 / 3;  // 3 (целочисленное деление отбрасывает дробную часть)
    int remainder = 10 % 3; // 1 (остаток от деления)

Особенности оператора % (остаток):
    - Может работать с дробными числами: 5.7 % 2.1
    - Знак результата равен знаку делимого (левого операнда):
        -5 % 2   = -1
        5 % -2   =  1

3.2. Инкремент (++) и декремент (--)

Увеличивают или уменьшают значение переменной на 1. Существуют в двух формах:

    ПРЕФИКСНАЯ (++x, --x): сначала изменяет значение, затем возвращает его.
    ПОСТФИКСНАЯ (x++, x--): сначала возвращает текущее значение, затем изменяет.

    int a = 5;
    int b = ++a;   // a = 6, b = 6 (префикс: увеличили a, потом присвоили)

    int c = 5;
    int d = c++;   // c = 6, d = 5 (постфикс: присвоили старое значение, потом увеличили)

ПРАКТИЧЕСКИЙ ПРИМЕР:
    int i = 0;
    while (i++ < 5) {
        System.out.println(i);   // выведет 1 2 3 4 5
    }
    // В условии используется старое значение i, но внутри цикла i уже увеличено.

3.3. Конкатенация строк через оператор +

Если хотя бы один операнд является строкой, оператор + выполняет склеивание
(конкатенацию), преобразуя второй операнд в строку автоматически.

    String text = "Ответ: " + 42;          // "Ответ: 42"
    String combined = 1 + 2 + "3";         // "33" (сначала 1+2=3, потом +"3")
    String combined2 = "1" + 2 + 3;        // "123" (слева направо: "1"+2="12", потом +3)

Порядок вычисления — слева направо.


4. ОПЕРАТОРЫ СРАВНЕНИЯ (RELATIONAL OPERATORS)

Всегда возвращают boolean (true или false).

    Оператор    Значение
    ------------------------------------------------------------
    ==          равно
    !=          не равно
    >           больше
    <           меньше
    >=          больше или равно
    <=          меньше или равно

    int a = 10;
    int b = 20;
    boolean result = a > b;    // false
    boolean equal = a == b;    // false

ВНИМАНИЕ: операторы == и != при работе с ссылочными типами (объектами) сравнивают
ССЫЛКИ (адреса в памяти), а не содержимое объектов.

    String s1 = new String("Hello");
    String s2 = new String("Hello");
    System.out.println(s1 == s2);        // false (разные объекты)
    System.out.println(s1.equals(s2));   // true  (сравнение содержимого)


5. ЛОГИЧЕСКИЕ ОПЕРАТОРЫ

Работают только с boolean операндами и возвращают boolean.

5.1. Базовые логические операторы

    Оператор    Название    Результат true, если
    ------------------------------------------------------------
    &           И (AND)     оба операнда true
    |           ИЛИ (OR)    хотя бы один true
    ^           XOR         операнды различны (true и false)
    !           НЕ (NOT)    операнд false (унарный)

    boolean a = true;
    boolean b = false;
    System.out.println(a & b);   // false
    System.out.println(a | b);   // true
    System.out.println(a ^ b);   // true  (разные)
    System.out.println(!a);      // false

5.2. Укороченные (short-circuit) версии: && и ||

    && (условное И) и || (условное ИЛИ) работают так же, как & и |, НО:
    - Если результат можно определить по первому операнду, второй НЕ ВЫЧИСЛЯЕТСЯ.

    Пример для &&:
        boolean result = (a != 0) && (10 / a > 2);
        // Если a == 0, первая часть false, вторая не вычисляется, деления на ноль не будет.

    Пример для ||:
        boolean result = (text == null) || text.isEmpty();
        // Если text == null, первая часть true, вторая не вычисляется, NPE не возникнет.

Когда использовать & и | вместо && и ||?
    - Только когда второй операнд должен быть вычислен ОБЯЗАТЕЛЬНО
      (например, содержит вызов метода с побочными эффектами).
    - В побитовых операциях с целыми числами.

5.3. Таблица истинности для логических операторов

    A       B       A & B   A | B   A ^ B   !A
    ------------------------------------------------------------
    true    true    true    true    false   false
    true    false   false   true    true    false
    false   true    false   true    true    true
    false   false   false   false   false   true


6. ПОБИТОВЫЕ ОПЕРАТОРЫ

Работают с целочисленными типами (byte, short, int, long) на уровне отдельных
битов. Применяются в низкоуровневом программировании, флагах, масках.

6.1. Побитовые логические операторы

    Оператор    Название    Действие
    ------------------------------------------------------------
    &           AND         бит = 1, если оба бита = 1
    |           OR          бит = 1, если хотя бы один = 1
    ^           XOR         бит = 1, если биты различны
    ~           NOT         инвертирует все биты (унарный)

    int a = 0b1100;   // 12 в десятичной
    int b = 0b1010;   // 10 в десятичной

    int and = a & b;  // 0b1000 (8)
    int or  = a | b;  // 0b1110 (14)
    int xor = a ^ b;  // 0b0110 (6)
    int not = ~a;     // инверсия всех 32 бит

6.2. Операторы сдвига

    Оператор    Название            Действие
    ------------------------------------------------------------
    <<          сдвиг влево         сдвигает биты влево, младшие биты заполняются нулями
    >>          сдвиг вправо        сдвигает биты вправо, старшие биты заполняются
                                    значением знакового бита (сохраняет знак)
    >>>         беззнаковый сдвиг   сдвигает биты вправо, старшие биты заполняются нулями
                вправо

    int x = 8;          // 0b0000...1000
    int left = x << 2;  // 32 (0b0010...0000) — эквивалентно умножению на 2^2
    int right = x >> 2; // 2  (0b0000...0010) — эквивалентно делению на 2^2

    int negative = -8;
    int signedShift = negative >> 2;   // -2 (знак сохранён)
    int unsignedShift = negative >>> 2;// 1073741822 (знак не сохранён, добавились нули)

Важно: при сдвиге типа int учитываются только младшие 5 бит правого операнда
(для long — 6 бит). То есть сдвиг на 33 эквивалентен сдвигу на 1.


7. ТЕРНАРНЫЙ ОПЕРАТОР (?:)

Единственный оператор в Java, принимающий три операнда.

Синтаксис:
    условие ? выражение1 : выражение2

    - Если условие истинно (true), вычисляется и возвращается выражение1
    - Если ложно (false) — выражение2

    int age = 20;
    String status = age >= 18 ? "Совершеннолетний" : "Несовершеннолетний";
    System.out.println(status);  // Совершеннолетний

    // Вложенные тернарные операторы (читаемо, но не злоупотребляйте):
    int score = 85;
    String grade = score >= 90 ? "A" :
                   score >= 80 ? "B" :
                   score >= 70 ? "C" : "F";

Типы выражений должны быть совместимы: компилятор определяет общий тип результата
по правилам продвижения числовых типов.

    int a = 5;
    double b = 10.5;
    double result = (a > 0) ? a : b;   // OK, результат double


8. ПРИОРИТЕТ ОПЕРАТОРОВ

Когда в выражении несколько операторов, они выполняются не слева направо, а в
соответствии с приоритетом. Операторы с более высоким приоритетом выполняются
раньше.

Таблица приоритетов (от высшего к низшему):

    Приоритет   Операторы
    ------------------------------------------------------------
    1 (высший)  ++ (постфикс), -- (постфикс)
    2           ++ (префикс), -- (префикс), + (унарный), - (унарный), !, ~, (type)
    3           *, /, %
    4           +, -
    5           <<, >>, >>>
    6           <, >, <=, >=, instanceof
    7           ==, !=
    8           & (побитовое)
    9           ^
    10          |
    11          &&
    12          ||
    13          ?:
    14 (низший) =, +=, -=, *=, /=, %=, &=, ^=, |=, <<=, >>=, >>>=

ПРАВИЛО: Если сомневаетесь в приоритете — ИСПОЛЬЗУЙТЕ СКОБКИ. Они повышают
читаемость и гарантируют нужный порядок.

    int result = 10 + 5 * 2;       // 20 (умножение раньше сложения)
    int result2 = (10 + 5) * 2;    // 30

    boolean test = a > 0 && b < 10 || c == 5;   // неочевидный порядок
    boolean clear = (a > 0 && b < 10) || c == 5; // читаемо


9. ОПЕРАТОР INSTANCEOF

Проверяет, является ли объект экземпляром указанного класса или его наследника
(или реализует интерфейс). Возвращает boolean.

    class Animal {}
    class Dog extends Animal {}

    Animal animal = new Dog();

    boolean isDog = animal instanceof Dog;       // true
    boolean isAnimal = animal instanceof Animal; // true
    boolean isString = animal instanceof String; // false (ошибка компиляции, если типы несовместимы)

    // Проверка на null
    Animal nullAnimal = null;
    System.out.println(nullAnimal instanceof Animal); // false (null не является экземпляром)

Новое в Java 14+ (Pattern Matching for instanceof):
    if (animal instanceof Dog dog) {
        // переменная dog уже создана и приведена к типу Dog
        dog.bark();
    }


10. ОПЕРАТОРЫ ДОСТУПА К ЧЛЕНАМ КЛАССА

10.1. Точечная нотация (.)

    Используется для доступа к полям и методам объекта или класса:
        object.method();
        ClassName.staticField;

10.2. Скобки [] для массивов

    int[] arr = new int[5];
    arr[0] = 10;                 // присваивание элементу
    int value = arr[0];          // чтение элемента

10.3. Оператор new

    Создаёт новый объект в куче и возвращает ссылку на него:
        String s = new String("Hello");
        int[] numbers = new int[10];
        Dog myDog = new Dog();


11. ТИПИЧНЫЕ ОШИБКИ И ПОДВОДНЫЕ КАМНИ

11.1. Путаница между == и .equals()
    String s1 = "Hello";
    String s2 = new String("Hello");
    if (s1 == s2) { ... }           // ОШИБКА: сравниваются ссылки

11.2. Деление целых чисел
    double result = 5 / 2;          // result = 2.0, а не 2.5
    // Правильно:
    double result2 = 5.0 / 2;       // 2.5

11.3. Переполнение (overflow)
    int max = Integer.MAX_VALUE;
    int overflow = max + 1;         // Integer.MIN_VALUE
    // Проверка:
    int result = Math.addExact(a, b); // бросит ArithmeticException при переполнении

11.4. Неправильное использование инкремента в выражениях
    int x = 5;
    int y = x++ + ++x;   // результат может быть неочевидным: 5 + 7 = 12
    // Избегайте сложных выражений с инкрементом

11.5. Забытый break в switch (рассмотрим позже, но важно)
    int day = 3;
    switch (day) {
        case 1: System.out.println("Mon");
        case 2: System.out.println("Tue");
        case 3: System.out.println("Wed"); // выполнится и все последующие, если нет break
    }

11.6. Приоритет логических операторов
    boolean res = true || false && false;   // true (&& выше приоритетом, чем ||)
    // Всегда используйте скобки: true || (false && false)


12. ПРИМЕРЫ ДЛЯ ЗАКРЕПЛЕНИЯ

Пример 1: Проверка на високосный год
    int year = 2024;
    boolean isLeap = (year % 4 == 0) && (year % 100 != 0 || year % 400 == 0);
    System.out.println(isLeap);  // true

Пример 2: Обмен значений без временной переменной (с помощью XOR)
    int a = 5;
    int b = 9;
    a = a ^ b;
    b = a ^ b;
    a = a ^ b;
    System.out.println("a = " + a + ", b = " + b);  // a = 9, b = 5

Пример 3: Битовые флаги
    int FLAG_A = 1;      // 0b001
    int FLAG_B = 2;      // 0b010
    int FLAG_C = 4;      // 0b100

    int options = FLAG_A | FLAG_C;            // установить флаги A и C
    boolean hasA = (options & FLAG_A) != 0;   // проверить флаг A
    options |= FLAG_B;                        // добавить флаг B
    options &= ~FLAG_A;                       // снять флаг A


13. ИТОГ ПО ТЕМЕ

- Операторы выполняют действия над операндами.
- Присваивание может быть комбинированным, с автоматическим приведением типов.
- Арифметические операторы работают по-разному с целыми и дробными числами.
- Инкремент/декремент имеют две формы, влияющие на возвращаемое значение.
- Логические операторы && и || "ленивые" — используют их по умолчанию.
- Побитовые операторы работают на уровне битов, полезны для флагов и оптимизаций.
- Тернарный оператор — компактная замена if-else.
- Приоритет операторов определяет порядок вычислений, скобки всегда надёжнее.
- Оператор instanceof проверяет тип объекта во время выполнения.

================================================================================`
    },
    // Добавьте остальные уроки по аналогии (Уроки 3-16)
    // Я добавлю заглушки для демонстрации, но вы можете скопировать сюда полный текст из ваших файлов.
    {
        id: 3,
        title: 'Условные операторы и циклы',
        description: 'if-else, switch, while, do-while, for, операторы break/continue.',
        content: `(Содержание урока 3. Условные операторы и циклы)`
    },
    {
        id: 4,
        title: 'Массивы',
        description: 'Одномерные и многомерные массивы, класс Arrays, особенности работы.',
        content: `(Содержание урока 4. Массивы)`
    },
    {
        id: 5,
        title: 'Строки (String, StringBuilder, StringBuffer)',
        description: 'Неизменяемость, методы, сравнение, конкатенация, String Pool, производительность.',
        content: `(Содержание урока 5. Строки)`
    },
    {
        id: 6,
        title: 'Методы',
        description: 'Сигнатура, параметры, возвращаемые значения, перегрузка, рекурсия, varargs, статические методы.',
        content: `(Содержание урока 6. Методы)`
    },
    {
        id: 7,
        title: 'Классы, объекты и конструкторы',
        description: 'Поля, методы, конструкторы, this, блоки инициализации, инкапсуляция, модификаторы доступа.',
        content: `(Содержание урока 7. Классы, объекты и конструкторы)`
    },
    {
        id: 8,
        title: 'Наследование, полиморфизм, абстрактные классы, интерфейсы',
        description: 'extends, super, переопределение, abstract, interface, default методы, функциональные интерфейсы.',
        content: `(Содержание урока 8. Наследование, полиморфизм, абстрактные классы и интерфейсы)`
    },
    {
        id: 9,
        title: 'Исключения',
        description: 'Иерархия, checked/unchecked, try-catch-finally, try-with-resources, throw/throws, создание своих исключений.',
        content: `(Содержание урока 9. Исключения)`
    },
    {
        id: 10,
        title: 'Generics (Обобщения)',
        description: 'Параметризованные классы и методы, wildcards (?), PECS, стирание типов, ограничения.',
        content: `(Содержание урока 10. Generics)`
    },
    {
        id: 11,
        title: 'Коллекции',
        description: 'List (ArrayList, LinkedList), Set (HashSet, TreeSet), Map (HashMap, TreeMap), Queue, итераторы, Comparable/Comparator.',
        content: `(Содержание урока 11. Коллекции)`
    },
    {
        id: 12,
        title: 'Stream API и Лямбда-выражения',
        description: 'Лямбды, функциональные интерфейсы, Stream API, промежуточные и терминальные операции, Collectors, parallel streams.',
        content: `(Содержание урока 12. Stream API и Лямбда-выражения)`
    },
    {
        id: 13,
        title: 'Многопоточность (Основы)',
        description: 'Thread, Runnable, состояния, синхронизация (synchronized, volatile), wait/notify, проблемы многопоточности.',
        content: `(Содержание урока 13. Многопоточность (Основы))`
    },
    {
        id: 14,
        title: 'Работа с датой и временем (Java Time API)',
        description: 'LocalDate, LocalTime, LocalDateTime, ZonedDateTime, Instant, Period, Duration, DateTimeFormatter, часовые пояса.',
        content: `(Содержание урока 14. Работа с датой и временем)`
    },
    {
        id: 15,
        title: 'Ввод и вывод (I/O и NIO)',
        description: 'Потоки (InputStream/OutputStream, Reader/Writer), работа с файлами через NIO (Path, Files), сериализация объектов.',
        content: `(Содержание урока 15. Ввод и вывод в Java)`
    },
    {
        id: 16,
        title: 'Аннотации и Рефлексия',
        description: 'Что такое аннотации, встроенные аннотации, мета-аннотации, создание своих аннотаций, обработка через Reflection API.',
        content: `(Содержание урока 16. Аннотации и Рефлексия в Java)`
    }
];
